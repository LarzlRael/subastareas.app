part of 'pages.dart';

class MyHomeworks extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('MyHomeworks'),
      ),
    );
  }
}

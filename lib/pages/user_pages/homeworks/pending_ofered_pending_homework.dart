part of '../../pages.dart';

class PendingOferedPendingHomework extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('PendingOferedPendingHomework'),
      ),
    );
  }
}
